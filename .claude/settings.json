{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -q '/root/venn/frontend/src/'; then cd /root/venn/frontend && npx eslint --fix \"$CLAUDE_FILE_PATH\" 2>/dev/null; fi",
            "timeout": 10000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -qE '\\.env($|\\.)'; then echo 'BLOCKED: .env files are protected — edit manually' >&2; exit 2; fi",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
